name: "cast_functions"
version: "1.0.0"
description: "Type casting and conversion function tests"
category: "cast"
total_tests: 144

testFiles:
  - name: "cast"
    file: "cast.test"
    description: "CAST function tests with error handling"
    estimated_tests: 76
  
  - name: "try_cast"
    file: "try_cast.test"
    description: "TRY_CAST function tests (safe casting)"
    estimated_tests: 68

notes: |
  Type casting is fundamental for data type conversions.
  Tests cover all primitive types and error/overflow handling.
  TRY_CAST returns NULL on failure instead of raising errors.

# Made with Bob
