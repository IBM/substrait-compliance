plugins {
    id 'org.gradle.toolchains.foojay-resolver-convention' version '0.8.0'
}

rootProject.name = 'substrait-compliance'

include 'sdk:java'
// api/ has its own standalone build (api/settings.gradle) because it requires
// Gradle 8 (Spring Boot 2.7 + io.spring.dependency-management is incompatible
// with Gradle 9).  Build api/ via: cd api && ./gradlew bootJar

// Made with Bob
